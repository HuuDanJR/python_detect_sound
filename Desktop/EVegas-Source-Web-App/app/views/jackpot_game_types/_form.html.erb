<%= form_with(model: jackpot_game_type, local: true) do |form| %>
  <div class="row">
    <% if jackpot_game_type.errors.any? %>
      <div class="col-md-12" id="error_explanation">
        <h4><%= pluralize(jackpot_game_type.errors.count, "error") %> prohibited this jackpot_game_type from being saved:</h4>

        <ul>
          <% jackpot_game_type.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  </div>

  <div class="row">
    <!-- left column -->
    <div class="col-md-12">
      <!-- general form elements -->
      <div class="card card-secondary">
        <div class="card-header">
          <h3 class="card-title">Information</h3>
        </div>
        <!-- /.card-header -->
        <div class="card-body">
          <div class="form-row">
            <div class="form-group col-sm-4">
              <label><%= t('jackpot_game_type_name') %>:</label>
              <%= form.text_field :name, class: "form-control", required: true %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-12">
      <%= link_to 'Back', 'javascript:void(0);', onclick: "window.history.back(); setTimeout(function(){window.location.reload();}, 100);", class: "btn btn-secondary" %>      <input class="btn btn-success float-right" name="btnSubmit" data-disable-with="Processing..." type="submit" value="Save" />
    </div>
  </div>
<% end %>
