<%= form_with(model: user_first_login, local: true) do |form| %>
  <% if user_first_login.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_first_login.errors.count, "error") %> prohibited this user_first_login from being saved:</h2>

      <ul>
        <% user_first_login.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
