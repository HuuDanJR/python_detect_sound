<%= form_with(model: product_category, local: true) do |form| %>
  <div class="row">
    <% if product_category.errors.any? %>
      <div class="col-md-12" id="error_explanation">
        <h4>Có <%= pluralize(product_category.errors.count, "error" ) %> errors:</h4>

        <ul>
          <% product_category.errors.full_product_categorys.each do |product_category| %>
            <li>
              <%= product_category %>
            </li>
            <% end %>
        </ul>
      </div>
      <% end %>
  </div>

  <div class="row">
    <div class="col-md-12">
      <div class="card card-secondary">
        <div class="card-header">
          <h3 class="card-title">Information</h3>
        </div>
        <div class="card-body">

          <div class="form-row">
            <div class="form-group col-sm-8">
              <label>
                <%= t('name') %>:
              </label>
              <%= form.text_field :name, class: "form-control", rows:"4", required: true %>
            </div>
            <div class="form-group col-sm-4">
              <label>
                <%= t('index') %>:
              </label>
              <%= form.text_field :index_category, class: "form-control", type: "number" %>
            </div>
          </div>

        </div>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-12">
      <%= link_to 'Back', 'javascript:void(0);', onclick: "window.history.back(); setTimeout(function(){window.location.reload();}, 100);", class: "btn btn-secondary" %>      
      <input class="btn btn-success float-right" name="btnSubmit" data-disable-with="Processing..." type="submit" value="Save" />
        <%# <button type="submit" class="btn btn-success float-right" id="btnSubmit">Save</button> %>
    </div>
  </div>
  <% end %>